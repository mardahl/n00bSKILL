# Packages

This folder contains upload-ready Claude skill packages.

## Available Package

- [`humanizer-danish.skill`](humanizer-danish.skill) — upload this in Claude from `Organization settings > Skills` or `Customize > Skills`.

The `.skill` file is the installable artifact. The matching source lives in [`../skills/humanizer-danish/`](../skills/humanizer-danish/).
